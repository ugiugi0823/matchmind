// types/metadata.d.ts

// 선수 정보 (spid.json)
export interface SpidEntry {
    id: number;
    name: string;
  }
  
  // 포지션 정보 (spposition.json)
  export interface SppositionEntry {
    spposition: number;
    desc: string;
  }
  